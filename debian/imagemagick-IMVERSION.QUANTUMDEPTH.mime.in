image/avs; magick-im${IMVERSION}.${QUANTUMDEPTH}. display 'avs:%s'; test=test -n "$DISPLAY"; priority=2
image/bie; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'jbig:%s'; test=test -n "$DISPLAY"; priority=2
image/x-ms-bmp; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'bmp:%s'; test=test -n "$DISPLAY"; priority=2
image/cmyk; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'cmyk:%s'; test=test -n "$DISPLAY"; priority=2
image/dcx; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'dcx:%s'; test=test -n "$DISPLAY"; priority=2
image/eps; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'eps:%s'; test=test -n "$DISPLAY"; priority=2
image/fax; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'fax:%s'; test=test -n "$DISPLAY"; priority=2
image/fits; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'fits:%s'; test=test -n "$DISPLAY"; priority=2
image/gif; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'gif:%s'; test=test -n "$DISPLAY"; priority=2
image/gray; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'gray:%s'; test=test -n "$DISPLAY"; priority=2
image/jpeg; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'jpeg:%s'; test=test -n "$DISPLAY"; priority=2
image/pjpeg; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'jpeg:%s'; test=test -n "$DISPLAY"; priority=2
image/miff; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'miff:%s'; test=test -n "$DISPLAY"; priority=2
image/mono; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'mono:%s'; test=test -n "$DISPLAY"; priority=2
image/mtv; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'mtv:%s'; test=test -n "$DISPLAY"; priority=2
image/x-portable-bitmap; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pbm:%s'; test=test -n "$DISPLAY"; priority=2
image/pcd; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pcd:%s'; test=test -n "$DISPLAY"; priority=2
image/pcx; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pcx:%s'; test=test -n "$DISPLAY"; priority=2
image/pdf; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pdf:%s'; test=test -n "$DISPLAY"; priority=2
image/x-portable-graymap; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pgm:%s'; test=test -n "$DISPLAY"; priority=2
image/pict; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pict:%s'; test=test -n "$DISPLAY"; priority=2
image/png; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'png:%s'; test=test -n "$DISPLAY"; priority=2
image/x-portable-anymap; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'pnm:%s'; test=test -n "$DISPLAY"; priority=2
image/x-portable-pixmap; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'ppm:%s'; test=test -n "$DISPLAY"; priority=2
image/ps; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'ps:%s'; test=test -n "$DISPLAY"; priority=2
image/rad; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'rad:%s'; test=test -n "$DISPLAY"; priority=2
image/x-rgb; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'rgb:%s'; test=test -n "$DISPLAY"; priority=2
image/rgba; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'rgba:%s'; test=test -n "$DISPLAY"; priority=2
image/rla; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'rla:%s'; test=test -n "$DISPLAY"; priority=2
image/rle; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'rle:%s'; test=test -n "$DISPLAY"; priority=2
image/sgi; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'sgi:%s'; test=test -n "$DISPLAY"; priority=2
image/sun-raster; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'sun:%s'; test=test -n "$DISPLAY"; priority=2
image/targa; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'tga:%s'; test=test -n "$DISPLAY"; priority=2
image/tiff; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'tiff:%s'; test=test -n "$DISPLAY"; priority=2
image/uyvy; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'uyvy:%s'; test=test -n "$DISPLAY"; priority=2
image/vid; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'vid:%s'; test=test -n "$DISPLAY"; priority=2
image/viff; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'viff:%s'; test=test -n "$DISPLAY"; priority=2
image/x-xbitmap; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'xbm:%s'; test=test -n "$DISPLAY"; priority=2
image/x-xpixmap; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'xpm:%s'; test=test -n "$DISPLAY"; priority=2
image/x-xwindowdump; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'xwd:%s'; test=test -n "$DISPLAY"; priority=2
image/x-icon; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'icon:%s'; test=test -n "$DISPLAY"; priority=2
image/yuv; magick-im${IMVERSION}.${QUANTUMDEPTH} display 'yuv:%s'; test=test -n "$DISPLAY"; priority=2
